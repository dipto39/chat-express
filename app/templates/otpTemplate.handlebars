<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Your OTP Code</title>
    <style>
      .container {
        max-width: 600px;
        margin: 0 auto;
        font-family: Arial, sans-serif;
      }
      .header {
        background-color: #4CAF50;
        padding: 10px;
        color: white;
        text-align: center;
      }
      .content {
        padding: 20px;
        text-align: center;
      }
      .otp-code {
        font-size: 24px;
        font-weight: bold;
        background-color: #f2f2f2;
        padding: 10px;
        margin: 20px 0;
        display: inline-block;
        letter-spacing: 5px;
      }
      .footer {
        text-align: center;
        font-size: 12px;
        color: #888;
        padding: 10px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h2>{{header}}</h2>
      </div>
      <div class="content">
        <p>Hello {{username}},</p>
        <p>Use the following OTP code to complete your verification:</p>
        <div class="otp-code">{{otpCode}}</div>
        <p>This OTP will expire in {{expiryTime}} minutes.</p>
      </div>
      <div class="footer">
        <p>If you didn't request this, please ignore this email.</p>
      </div>
    </div>
  </body>
</html>
